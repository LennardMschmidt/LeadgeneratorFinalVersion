# Leave empty in local dev to use Vite proxy (/api -> http://127.0.0.1:4000 by default).
# Set in production builds when frontend and backend are on different domains.
VITE_API_BASE_URL=

# Local dev proxy target (used by vite.config.ts only)
VITE_API_PROXY_TARGET=http://127.0.0.1:4000

# Supabase Auth (required for login/register modal)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
