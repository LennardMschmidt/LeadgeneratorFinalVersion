export const HOSTING_CONFIG = {
  // Static hosting branch: keep config in code (no env files required).
  apiBaseUrl: '/api',
  supabaseUrl: '',
  supabasePublishableKey: '',
  supabaseAnonKey: '',
} as const;

